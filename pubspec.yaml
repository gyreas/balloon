name: balloon
description: "A Spotlight alternative for Linux and Android"
publish_to: 'none'
version: 0.1.8

environment:
  sdk: 3.7.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 5.0.0

flutter:
  uses-material-design: true
